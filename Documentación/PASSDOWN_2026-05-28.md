# Passdown — MaduraApp-Produccion · 2026-05-28

> MaduraApp es un sistema de análisis de madurez agrícola con visión computacional: app Android (CameraX + Room + MVVM) + API FastAPI + modelo YOLO26n. La Evaluación 2 (examen del jueves 28/05) ya pasó. **El foco de la PRÓXIMA sesión es continuar las nuevas implementaciones: autenticación JWT + feedback con rating, que viven en la rama `feature/auth-feedback`.**
> Lee este archivo antes de empezar. Última actualización: 2026-05-28.

---

## Project state

### Complete (en `main`, estable)

- **Backend FastAPI** — 3 endpoints (`/v1/predict`, `/v1/history`, `/v1/health`) + YOLO26n + SQLAlchemy async + Alembic + 9/9 tests pytest
- **App Android** — FruitSelectorActivity (LAUNCHER) + MainActivity (CameraX + galería) + HistoryActivity + MVVM + Room offline + Retrofit
- **Modelo YOLO26n** — mAP@50 = 0.9229, 5.2 MB, 12 clases, 4 frutas. Archivo en `Producto/backend/weights/yolo26n_maduraapp.pt`
- **Build Android desde CLI** — `gradlew` + `gradlew.bat` commiteados; dependencia `gridlayout:1.0.0`; `activity_main.xml` con `actionButtons` + `btnGallery` (FIX aplicado en commit 559ea70)
- **Documentación Evaluación 2** — 14 MDs (4+1 Kruchten), informe Word, PPT 9 slides con notas de orador, documento 1.1.2 de registro
- **Deploy** — `render.yaml` (Render Free + Docker) + Supabase PostgreSQL configurado
- **Túnel para demo remota** — `cloudflared tunnel --url http://localhost:8000` (probado, funciona sin bypass page)

### Pending — NUEVAS IMPLEMENTACIONES (foco próxima sesión)

La rama **`feature/auth-feedback`** ya tiene implementado (commit `263f76a`):
- **Backend JWT auth** — `POST /v1/auth/register`, `POST /v1/auth/login`, `GET /v1/auth/me`; tabla `users`; bcrypt; python-jose; migración Alembic `0002`
- **Backend feedback** — `POST /v1/feedback` (rating 1-5 estrellas por scan_id); tabla `scan_feedback`; `/v1/predict` y `/v1/history` ahora requieren JWT y filtran por user_id
- **Android auth** — LoginActivity (nuevo LAUNCHER), RegisterActivity, TokenManager, AuthPreferences (DataStore), AuthInterceptor (agrega JWT automáticamente), AuthRepository, AuthViewModel
- **Android feedback** — RatingBottomSheet (5 estrellas, aparece 1.5s post-escaneo)

**⚠️ PROBLEMA CRÍTICO #1:** `feature/auth-feedback` se ramificó de `main` ANTES de los fixes de build de Android (gradlew, gridlayout, activity_main `actionButtons`/`btnGallery`). **Si intentas compilar el APK de esa rama, FALLARÁ** con "Unresolved reference btnGallery/actionButtons" y "gradlew not found". → **Primer paso obligatorio: hacer merge de `main` → `feature/auth-feedback` (o rebase) para traer esos fixes antes de compilar.**

**⚠️ PROBLEMA CRÍTICO #2:** En la rama auth-feedback, los tests del backend probablemente fallan porque `/v1/predict` y `/v1/history` ahora exigen JWT, pero los tests viejos (`test_predict.py`, `test_history.py`) no envían token válido. → Hay que actualizar los tests para registrar/loguear un usuario y usar su token.

---

## This session — what happened

### Topics discussed

1. **Demo en sala / acceso remoto** — Se configuró ADB en PATH (`adb reverse tcp:8000 tcp:8000` para teléfono físico + backend local). Para acceso desde datos móviles se probó localtunnel (inestable), ngrok (requiere cuenta) y finalmente **cloudflared** (funciona bien, sin bypass page). La URL de cloudflared cambia cada sesión → hay que recompilar el APK con la nueva URL.
2. **Build del APK desde CLI** — Se descubrió que `gradlew` NO estaba en el repo. Se generó con el Gradle cacheado de Android Studio. Faltaba dependencia `gridlayout`. El `activity_main.xml` viejo no tenía `btnGallery` ni `actionButtons` (el MainActivity sí los referenciaba) → se agregaron. APK compila e instala OK ahora.
3. **pytest** — Se corre desde `Producto/backend` con venv activado: `pytest tests/ -v`. Hubo que cambiar `asyncpg==0.29.0` → `>=0.29.0` por incompatibilidad con Python 3.13 (no tiene wheel precompilado).
4. **Limpieza de Git** — Se eliminaron los `Co-Authored-By` del historial con `git filter-repo`. **El usuario NO quiere Co-Authored-By en los commits.** `CLAUDE.md` se quitó del repo y está en `.gitignore`.
5. **Entrega Evaluación 2** — Se creó `EP_TPY1101_ClaudioAro_001D.rar` (en `C:\Users\molte\Documents\`). `Integrantes.txt` en `Gestión/`.
6. **Documento 1.1.2 de registro** — Editado con datos de MaduraApp y guardado en `Gestión/1.1.2 Documento de registro MaduraApp.docx`. Cuidado: el campo "Descripción" aparecía 5 veces en el doc; hubo que reemplazar SOLO la 2da ocurrencia (la celda de datos del Plan de Trabajo), no las etiquetas de sección.

### Files created or modified (esta sesión, en `main`)

| File | What changed |
|---|---|
| `Producto/frontend/gradlew` + `gradlew.bat` | Creados (faltaban para compilar desde CLI) |
| `Producto/frontend/app/build.gradle.kts` | + `androidx.gridlayout:gridlayout:1.0.0` |
| `Producto/frontend/app/src/main/res/layout/activity_main.xml` | + `actionButtons` LinearLayout con `btnScan` + `btnGallery` |
| `Producto/backend/requirements.txt` | `asyncpg==0.29.0` → `>=0.29.0` (Python 3.13) |
| `Gestión/Integrantes.txt` | Creado (Claudio Vicente Aro Kath, RUT 22.022.498-8) |
| `Gestión/1.1.2 Documento de registro MaduraApp.docx` | Documento de registro editado con datos del proyecto |
| `Documentación/Evaluacion_2/` | Informe Word con diagramas, PPT con notas de orador |
| `.gitignore` | + `CLAUDE.md` |

### Decisions made

| Decisión | Reasoning |
|---|---|
| cloudflared para acceso remoto | Más estable que localtunnel, sin cuenta (ngrok la pide), sin bypass page |
| NO usar Co-Authored-By en commits | Preferencia explícita del usuario; ya limpiado del historial con filter-repo |
| Las nuevas features (JWT+feedback) viven en `feature/auth-feedback` | Mantener `main` estable para la entrega; mergear post-examen |

---

## Key technical facts

```
Repo local:  C:\Users\molte\Documents\Proyectos\MaduraApp-Produccion
GitHub:      https://github.com/apotheosisss/MaduraApp-Produccion
Estudiante:  Claudio Vicente Aro Kath — RUT 22.022.498-8 — Sección 001D
Docente:     José Ignacio Campos Arévalo
Asignatura:  TPY1101 — Taller Aplicado de Programación (DuocUC) — PROYECTO INDIVIDUAL

Ramas:
  main                       → estable, entregado en Eval 2
  feature/auth-feedback      → JWT auth + rating feedback (FOCO próxima sesión)
  backup/2026-05-26-pre-sync → respaldo

Endpoints actuales (main):
  POST /v1/predict   — imagen + fruit_type opcional → ScanResult
  GET  /v1/history   — historial paginado
  GET  /v1/health    — {status, model, version, model_loaded}

Endpoints NUEVOS (feature/auth-feedback):
  POST /v1/auth/register  — {username, email, password} → JWT
  POST /v1/auth/login     — {email, password} → JWT
  GET  /v1/auth/me        — info del usuario (valida token)
  POST /v1/feedback       — {scan_id, rating 1-5} (requiere JWT)
  (predict e history ahora requieren JWT y filtran por user_id)

Modelo: YOLO26n, 5.2 MB, mAP@50 = 0.9229
4 frutas: aguacate_hass | platano | tomate_usda | mango
3 estados: INMADURO | OPTIMO | SOBRE_MADURO
CONFIDENCE_THRESHOLD = 0.55 (× 0.5 cuando hay fruit_filter)

Herramientas locales:
  ADB:    C:\Users\molte\AppData\Local\Android\Sdk\platform-tools\adb.exe
  SDK:    C:\Users\molte\AppData\Local\Android\Sdk
  JDK:    C:\Program Files\Android\Android Studio\jbr
  Gradle: cacheado en ~/.gradle/wrapper/dists/gradle-8.9-bin
```

---

## Next steps (foco: nuevas implementaciones)

1. **Mergear `main` → `feature/auth-feedback`** para traer los fixes de build de Android (gradlew, gridlayout, activity_main). Sin esto el APK de la rama NO compila.
   ```bash
   git checkout feature/auth-feedback
   git merge main      # resolver conflictos en activity_main.xml, build.gradle.kts si los hay
   ```
2. **Arreglar los tests del backend en la rama** — `test_predict.py` y `test_history.py` fallarán porque ahora se requiere JWT. Hay que registrar un usuario en un fixture y enviar el token en los headers. Correr `pytest tests/ -v` hasta tener verde.
3. **Verificar la migración Alembic 0002** (tablas `users` + `scan_feedback`) — correr `alembic upgrade head` contra la BD local y confirmar que crea las tablas.
4. **Compilar e instalar el APK de la rama** y probar el flujo completo: registro → login → escaneo → rating 1-5 estrellas.
5. **Integrar RatingBottomSheet visualmente** — confirmar que aparece 1.5s después de un escaneo exitoso y que el POST /v1/feedback funciona.
6. Cuando todo esté verde y probado → **merge `feature/auth-feedback` → `main`** vía PR o directo.

---

## Context for next Claude session

El usuario (Claudio) es estudiante de DuocUC trabajando SOLO en MaduraApp. La Evaluación 2 (con `main`) ya se entregó y defendió. Ahora quiere avanzar de lleno en las NUEVAS funcionalidades que ya están codeadas pero sin integrar/probar en la rama `feature/auth-feedback`: autenticación JWT y feedback con rating de estrellas. El trabajo NO es escribir esas features desde cero (ya existen) sino **integrarlas, arreglar los tests, mergear con los fixes de build de main, y dejarlas funcionando end-to-end**. NO agregar `Co-Authored-By` a los commits. El usuario compila el APK desde CLI (no abre Android Studio) y prueba en su teléfono físico vía ADB o cloudflared. El repo de testing original (MaduraApp) ya no se usa; todo el trabajo va a MaduraApp-Produccion.

---

## Quick reference

```bash
# Cambiar a la rama de trabajo y traer fixes de main
git checkout feature/auth-feedback
git merge main

# Backend local (Git Bash desde producto/backend)
source .venv/Scripts/activate
alembic upgrade head        # aplica migración 0002 (users + feedback)
uvicorn app.main:app --host 0.0.0.0 --port 8000

# Tests backend
pytest tests/ -v            # OJO: hay que arreglarlos para JWT en esta rama

# Compilar APK (PowerShell) — requiere los fixes de main mergeados
$env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"
$env:ANDROID_HOME = "$env:LOCALAPPDATA\Android\Sdk"
cd Producto/frontend
.\gradlew assembleDebug

# Instalar APK en teléfono
adb install -r app/build/outputs/apk/debug/app-debug.apk

# Acceso remoto (datos móviles) — la URL cambia cada vez
cloudflared tunnel --url http://localhost:8000
# → copiar la URL https://xxx.trycloudflare.com → ponerla en
#   Producto/frontend/gradle.properties (maduraapp.api.baseUrl) → recompilar
```

```
gradle.properties (local, NO commitear con URL de túnel):
  maduraapp.api.baseUrl=http://localhost:8000/        # demo con adb reverse
  maduraapp.api.baseUrl=https://xxx.trycloudflare.com/ # demo datos móviles

Variables de entorno backend nuevas (feature/auth-feedback):
  JWT_SECRET_KEY=<secret>      # firma de los tokens
  JWT_ALGORITHM=HS256
  JWT_EXPIRE_MINUTES=<n>
  (revisar Producto/backend/app/core/config.py en la rama)
```

---

*Generado el 2026-05-28 con el skill /passdown*
