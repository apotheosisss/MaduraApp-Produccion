# Passdown — MaduraApp-Produccion · 2026-05-27

> MaduraApp es un sistema de análisis de madurez agrícola con visión computacional: app Android (CameraX + Room + MVVM) + API FastAPI + modelo YOLO26n. Este es el repositorio de producción/título académico (TPY1101 · DuocUC). Lee este archivo antes de empezar. Última actualización: 2026-05-27.

---

## Project state

### Complete

- **Backend FastAPI** — 3 endpoints (predict, history, health) + YOLO26n + SQLAlchemy async + Alembic + 9/9 tests pytest
- **App Android** — FruitSelectorActivity (pantalla inicial) + CameraX + galería + MVVM + Room offline + Retrofit
- **Modelo YOLO26n** — mAP@50 = 0.9229, 5.2 MB, 12 clases, 4 frutas
- **Documentación Evaluación 2** — 14 archivos MD (4+1 Kruchten, configs, tests, backup, etc.) + Word + PPT (9 slides)
- **Deploy config** — `render.yaml` para Render + Supabase PostgreSQL configurado
- **Demo tools** — `DEMO_INICIO.ps1` (setup automático) + `GUIA_DEMO_SALA.md`
- **Entrega** — `EP_TPY1101_ClaudioAro_001D.rar` en `C:\Users\molte\Documents\`
- **Tunnel tools** — `cloudflared` instalado y probado, `localtunnel` disponible como fallback
- **gradlew** — agregado al repo (faltaba), ahora se puede compilar desde CLI
- **Dependencias Android** — `gridlayout:1.0.0` agregada; `activity_main.xml` actualizado con `actionButtons` + `btnGallery`
- **feature/auth-feedback** — rama con JWT auth + rating 1-5 estrellas lista para después del examen
- **asyncpg** — actualizado a `>=0.29.0` para compatibilidad con Python 3.13

### Pending

- **⚠️ gradle.properties tiene URL de cloudflare temporal** — `https://unknown-strategic-internet-specializing.trycloudflare.com/` — debe restaurarse a `http://localhost:8000/` antes de la demo del jueves
- **Evaluación oral jueves 2026-05-28** — demo en vivo en sala del laboratorio
- **APK para la demo** — recompilar con `localhost:8000` e instalar en teléfono esta noche
- **platform-tools en USB** — llevar para `adb reverse tcp:8000 tcp:8000` en sala

---

## This session — what happened

### Topics discussed

1. **Clonado MaduraApp-Produccion** — clonado en `C:\Users\molte\Documents\Proyectos\MaduraApp-Produccion` como repo principal de trabajo.
2. **Sincronización completa** — propagados todos los cambios de MaduraApp (testing) al repo de producción (FruitSelectorActivity, galería, TTA, threshold 0.55, recommendations, etc.).
3. **Documentación Evaluación 2** — generados 14 archivos MD con Vista Lógica, Procesos, Desarrollo, Física y Escenarios (+1) de Kruchten; Plan de Pruebas; Configuración ambientes; Backup. Informe Word y PPT (9 slides, paleta verde).
4. **Demo sala** — creados `DEMO_INICIO.ps1` + `GUIA_DEMO_SALA.md`. ADB configurado en PATH. Estrategia: `localhost:8000` + `adb reverse tcp:8000 tcp:8000`.
5. **RAR de entrega** — creado `EP_TPY1101_ClaudioAro_001D.rar` (9.5 MB) en `C:\Users\molte\Documents\`. Sección: 001D, Docente: José Ignacio Campos Árevalo.
6. **Tunnel para acceso remoto** — probados localtunnel (inestable), ngrok (requiere cuenta), cloudflared (funciona bien). Flujo: `cloudflared tunnel --url http://localhost:8000` → nueva URL → yo recompilo el APK con esa URL.
7. **Fixes Android build** — `gradlew` no estaba en el repo (generado y commiteado). Faltaba `gridlayout:1.0.0`. `activity_main.xml` no tenía `btnGallery` ni `actionButtons` (agregados).
8. **README actualizados** — raíz y `Producto/README.md` reescritos con estado actual del proyecto.
9. **Co-Authored-By eliminados** — `git filter-repo` aplicado; historial limpio.
10. **CLAUDE.md removido** del repo (en `.gitignore`).

### Files created or modified

| File | What changed |
|---|---|
| `Producto/frontend/gradle.properties` | URL temporal de cloudflare — **RESTAURAR a localhost:8000** |
| `Producto/frontend/app/build.gradle.kts` | Agregada dependencia `gridlayout:1.0.0` |
| `Producto/frontend/app/src/main/res/layout/activity_main.xml` | Agregados `actionButtons` LinearLayout + `btnGallery` |
| `Producto/frontend/gradlew` + `gradlew.bat` | Creados (faltaban) |
| `Producto/frontend/local.properties` | Creado localmente con SDK path (en .gitignore, no commiteado) |
| `Documentación/Evaluacion_2/` | 14 MDs + Word + PPT completos |
| `Gestión/Integrantes.txt` | Creado con nombre y RUT del estudiante |
| `README.md` (raíz) | Reescrito completo |
| `Producto/README.md` | Reescrito completo |
| `DEMO_INICIO.ps1` | Creado — script de setup automático para sala |
| `GUIA_DEMO_SALA.md` | Creado — guía paso a paso para defensa |
| `.gitignore` | Agregado `CLAUDE.md` |
| `Producto/backend/requirements.txt` | asyncpg `==0.29.0` → `>=0.29.0` |

### Decisions made

| Decisión | Razonamiento |
|---|---|
| Trabajar solo en MaduraApp-Produccion | Agilizar flujo, MaduraApp (testing) queda como referencia |
| cloudflared sobre localtunnel/ngrok | Más estable, sin cuenta, sin bypass page |
| Recompilar APK cada vez que cambia la URL del túnel | URL de cloudflare cambia con cada sesión; no hay otra opción en tier gratuito |
| gradlew commiteado al repo | Permite compilar desde CLI sin necesitar Android Studio abierto |
| localhost:8000 como URL base para demo | Se usa con `adb reverse` — no depende de red WiFi del laboratorio |

---

## Key technical facts

```
Repo local:  C:\Users\molte\Documents\Proyectos\MaduraApp-Produccion
GitHub:      https://github.com/apotheosisss/MaduraApp-Produccion
Rama activa: main (para examen)
Rama futura: feature/auth-feedback (JWT + rating, post-examen)

Estudiante: Claudio Vicente Aro Kath — RUT: 22.022.498-8
Docente:    José Ignacio Campos Árevalo
Sección:    001D
Asignatura: TPY1101 — Taller Aplicado de Programación

Endpoints backend:
  POST /v1/predict   — imagen + fruit_type opcional → ScanResult
  GET  /v1/history   — historial paginado
  GET  /v1/health    — {status, model_loaded: bool}

Modelo: YOLO26n, 5.2 MB, mAP@50 = 0.9229
Peso:   Producto/backend/weights/yolo26n_maduraapp.pt (en Git)

4 frutas: aguacate_hass | platano | tomate_usda | mango
3 estados: INMADURO | OPTIMO | SOBRE_MADURO
CONFIDENCE_THRESHOLD = 0.55 (threshold × 0.5 con fruit_filter activo)

ADB:     C:\Users\molte\AppData\Local\Android\Sdk\platform-tools\adb.exe
SDK:     C:\Users\molte\AppData\Local\Android\Sdk
JDK:     C:\Program Files\Android\Android Studio\jbr
Gradle:  C:\Users\molte\.gradle\wrapper\dists\gradle-8.9-bin\...\bin\gradle.bat

Compilar APK:
  $env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"
  $env:ANDROID_HOME = "$env:LOCALAPPDATA\Android\Sdk"
  cd Producto/frontend
  .\gradlew assembleDebug

Instalar APK:
  adb install -r app/build/outputs/apk/debug/app-debug.apk

Tunnel cloudflare:
  cloudflared tunnel --url http://localhost:8000
  → URL cambia cada sesión → recompilar APK con nueva URL

RAR entrega:  C:\Users\molte\Documents\EP_TPY1101_ClaudioAro_001D.rar (9.5 MB)
```

---

## Next steps

1. **URGENTE — Esta noche:** Restaurar `gradle.properties` a `http://localhost:8000/` → compilar APK → instalar en teléfono → probar con `adb reverse tcp:8000 tcp:8000`
2. **Esta noche:** Llevar `platform-tools/` en USB (en `C:\Users\molte\AppData\Local\Android\Sdk\platform-tools\`) para `adb reverse` en sala del laboratorio
3. **Mañana (jueves 28/05):** Defensa oral Evaluación 2 — demo en vivo con backend local + `adb reverse`
4. **Post-examen:** Hacer merge de `feature/auth-feedback` → `main` (JWT auth + rating 1-5 estrellas)
5. **Post-examen:** Considerar Render Standard ($25/mo) si el proyecto continúa

---

## Context for next Claude session

El usuario es Claudio Vicente Aro Kath, estudiante de DuocUC, sección 001D, con la evaluación oral el jueves 2026-05-28. El proyecto está completo y listo para la defensa. El único pendiente crítico es restaurar la URL de gradle.properties y recompilar el APK para la demo. La estrategia de demo usa backend local en el PC de la sala + `adb reverse tcp:8000 tcp:8000` para conectar el teléfono sin depender del WiFi. Para acceso remoto desde cualquier lugar, se usa `cloudflared tunnel --url http://localhost:8000` y se recompila el APK con la URL nueva cada sesión.

---

## Quick reference

```bash
# Backend local
cd Producto/backend && source .venv/Scripts/activate
uvicorn app.main:app --host 0.0.0.0 --port 8000

# Tunnel cloudflare (acceso remoto desde celular)
cloudflared tunnel --url http://localhost:8000
# → copiar URL → pasar a Claude → recompila APK automáticamente

# Tests
cd Producto/backend && pytest tests/ -v

# Compilar APK (PowerShell)
$env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"
$env:ANDROID_HOME = "$env:LOCALAPPDATA\Android\Sdk"
cd Producto/frontend && .\gradlew assembleDebug

# Instalar APK
adb install -r app/build/outputs/apk/debug/app-debug.apk

# Demo sala — orden obligatorio
# 1. Backend corriendo → 2. adb reverse tcp:8000 tcp:8000 → 3. Abrir app

# IMPORTANTE: restaurar antes del examen
# gradle.properties → maduraapp.api.baseUrl=http://localhost:8000/
```

```
Ramas:
  main             → producción / examen (estable)
  feature/auth-feedback → JWT + rating post-examen
  backup/2026-05-26-pre-sync → respaldo pre-sincronización

Documentación:
  Documentación/Evaluacion_2/00_Informe_Tecnico_Evaluacion_2.md (maestro)
  Documentación/Evaluacion_2/Informe_Evaluacion_2_MaduraApp.docx
  Documentación/Evaluacion_2/MaduraApp_Presentacion_Evaluacion2.pptx

Entrega:
  C:\Users\molte\Documents\EP_TPY1101_ClaudioAro_001D.rar
```

---

*Generado automáticamente el 2026-05-27 con el skill /passdown*
